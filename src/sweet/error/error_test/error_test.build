
for _, build in build:default_builds("cc_.*") do 
    build:all {
        build:Executable '${bin}/error_test_${architecture}' {
            '${lib}/error_${architecture}';
            '${lib}/assert_${architecture}';
            '${lib}/UnitTest++_${architecture}';

            build:Cxx () {
                precompiled_header = "stdafx.hpp";
                "main.cpp"
            };
        };
    };
end
