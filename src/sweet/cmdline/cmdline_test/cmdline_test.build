
local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    Executable ("cmdline_test", architecture) {
        libraries = {
            "sweet/cmdline/cmdline",
            "sweet/error/error",
            "sweet/assert/assert",
            "unit/libUnitTest"
        };

        Cxx (architecture) {
            "main.cpp",
            "TestParser.cpp",
            "TestSplitter.cpp"
        };
    };
end
