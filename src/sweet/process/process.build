
buildfile "process_test/process_test.build";

local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    Library ("process", architecture) {
        libraries = {
            "sweet/assert/assert",
            "sweet/error/error"
        };

        Cxx (architecture) {
            precompiled_header = "stdafx.hpp";
            "Error.cpp",
            "Process.cpp"
        };
    };
end
