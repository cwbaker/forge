
local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    build.Executable ("path_test", architecture) {
        libraries = {
            "sweet/assert/assert", 
            "sweet/error/error",
            "sweet/path/path",
            "unit/libUnitTest"
        };

        build.Cxx (architecture) {
            precompiled_header = "stdafx.hpp";
            "main.cpp",
            "TestPath.cpp",
            "TestWidePath.cpp"
        };
    };
end
