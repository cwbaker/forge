
local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    Executable ("pointer_test", architecture) {
        libraries = {
            "sweet/pointer/pointer",
            "sweet/error/error",
            "sweet/assert/assert",
            "unit/libUnitTest"
        };

        Cxx (architecture) {
            "main.cpp",
            "TestPointer.cpp",
            "TestPointerCast.cpp"
        };
    };
end
