
for _, build in build:default_builds("cc_.*") do
    build:Library "luaxx" {
        libraries = {
            "lua/liblua",
            "sweet/assert/assert",
        };

        build:Cxx () {
            "luaxx.cpp"
        };
    };
end
