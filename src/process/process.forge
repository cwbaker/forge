
buildfile "process_test/process_test.forge";

for _, forge in forge:default_builds("cc_.*") do
    forge:Library '${lib}/process_${architecture}' {
        forge:Cxx '${obj}/%1' {
            "Error.cpp",
            "Environment.cpp",
            "Process.cpp"
        };
    };
end
